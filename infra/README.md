# OpenTofu

This directory owns only resources for `pro-angle-construction-site`. It deliberately contains no data source or resource for the estimate application.

Use a remote encrypted state backend before team operation. Local state and tfvars files are ignored for bootstrap only. Always inspect the plan for changes outside the six expected resource groups: repository, vulnerability alerts, ruleset, public preview policy/applications, and public-site custom domains.

